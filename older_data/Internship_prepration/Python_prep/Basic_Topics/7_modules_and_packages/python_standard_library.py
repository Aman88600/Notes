# The python library provides us with many predefined funciton, variables, classes etc
# One example is the math module
import math
for i in range(1, 40, 1):
    print(f"i = {i/10} sin(i) = {math.sin(i)/10}") # Here we are using the sine function i.e. prefefined in the math module